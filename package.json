{
  "name": "Meta-Pages-FE",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "main": "index.js",
  "scripts": {
    "meta-space-dev": "lerna exec --scope meta-space -- yarn dev",
    "meta-space-lint": "lerna exec --scope meta-space -- yarn lint",
    "meta-space-build": "lerna exec --scope meta-space -- yarn build",
    "meta-space-start": "lerna exec --scope meta-space -- yarn start"
  },
  "repository": "https://github.com/Meta-Network/Meta-Pages-FE.git",
  "author": "xiaotiandada <istianlei@qq.com>",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
